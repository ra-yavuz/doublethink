<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Declared up front for later wiring; not all are used by the placeholder yet. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:label="doublethink"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doublethink">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Doublethink">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Holds live broker connections so messages arrive while the app is backgrounded.
             specialUse: this maintains persistent pub/sub connections for message delivery;
             doublethink has no push gateway, so a held connection is the delivery mechanism. -->
        <service
            android:name=".service.SubscriptionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Maintains live pub/sub connections to deliver messages; no push gateway exists." />
        </service>

    </application>

</manifest>
