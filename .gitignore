# Editor / OS
*~
*.swp
.DS_Store

# Local
build/
dist/

# Secrets / local config and peer identity files (never commit)
*.key
*.pem
.env
.env.*
!.env.example
*-identity.json
agent.json
pwa.json
*.state.json
state.json

# Shell / tooling state that lands in the working dir
.bash_history
.claude/

# Go build output
/doublethink
*.test
*.out
