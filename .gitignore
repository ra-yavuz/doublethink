# Editor / OS
*~
*.swp
.DS_Store

# Local
build/
dist/

# Secrets / local config and peer identity files (never commit)
*.key
*.pem
.env
.env.*
!.env.example
*-identity.json
agent.json
pwa.json
*.state.json
state.json

# Shell / tooling state that lands in the working dir
.bash_history
.claude/

# Go build output
/doublethink
*.test
*.out

# deploy-only, not committed (push script, stack, nginx)
portainer/

# Internal planning / design-process docs: kept locally, NOT published.
# (Public surface is the README + docs/SECURITY.md + the code.)
/GOAL.md
/docs/DESIGN-M1.md
/docs/DESIGN-M2.md
/docs/RESEARCH.md
/docs/CODESPEAK-REQUIREMENTS.md
